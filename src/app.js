// Aqui você poderia montar header dinamicamente se quisesse.
// Como seu header já está no index.html, só deixamos um espaço para
// hooks globais no futuro (ex.: checar token, etc.).
document.addEventListener('DOMContentLoaded', () => {
  // Ex.: checar se existe token em store e redirecionar:
  // if (store.authToken) location.hash = '#/dashboard';
});
